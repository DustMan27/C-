#include <iostream>
using namespace std;

int main() {
    int x = 5;
    int y = 9;
    if (x > y){
        cout << x << " is bigger than " << y;
    } else {
        cout << y << " is bigger than " << x;
    }
    return 0;
    }
    cout << "Please enter a new value for value " << y << "also known as y:";
    cin >> y;
    if (x > y){
      cout << x << " is bigger than " << y;
    } else {
      cout << y << " is bigger than " << x;
    return 0;
    }
  }
